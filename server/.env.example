# Server port (41xx where xx = last two digits of your student ID)
PORT=4100

# Session secret (change this to a random string)
SESSION_SECRET=change-me-to-a-random-string

# MariaDB connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=gradpath
